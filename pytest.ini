[pytest]
testpaths = tests
addopts = -q -p no:cacheprovider
faulthandler_timeout = 180
faulthandler_exit_on_timeout = true
markers =
  smoke: smoke tests (fast, deterministic, no network)
  unit: unit tests
